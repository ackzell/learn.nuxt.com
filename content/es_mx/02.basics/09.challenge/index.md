---
title: Challenge
isChallenge: true
---

# Challenge!

::challenge
Fix the bugs in this app
::

Okay, so hopefully you have already played around in this interactive environment. But if not, this is the perfect time to do so.

On the right, you have an `index.html` file that is not quite working as you can tell from the **Preview** panel. I'll tell you what _we are doing_ and the expected result, then you can figure out the solution.

- We are correctly importing Vue
- We set up the import maps correctly
- We are creating an app instance via `createApp`

Now, we need to make sure we get something similar to:

```txt showLineNumbers=false
Hola Vue!
Current Version is: 3.5.22
```

Note that the actual version might change as new versions of Vue are released since I wrote this.
